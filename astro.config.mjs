import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    //site: 'https://pablocevalenzuela.github.io',
    //base: '/pablocevalenzuela.github.io',
});
